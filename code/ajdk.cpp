#include<bits/stdc++.h>
using namespace std;
typedef long long int ll;
struct data{
    char name[1000];
    int w,h;
    ll income;
    bool operator<(const int & b)const{
        if(income==b.income){
            if(h==b.h){
                if(w==b.w)
                {
                    if(strlen(name)>)
                }
            }
        }
    }
};
int main(){

}
